pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls'
                sh 'cd sensor_interface'
                sh 'ls'

            }
        }

    }
}